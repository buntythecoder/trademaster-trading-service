<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property definitions -->
    <springProfile name="!prod">
        <property name="LOG_LEVEL" value="INFO"/>
        <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    </springProfile>
    
    <springProfile name="prod">
        <property name="LOG_LEVEL" value="INFO"/>
    </springProfile>
    
    <!-- Console appender for development -->
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
            </encoder>
        </appender>
    </springProfile>
    
    <!-- Structured JSON appender for production -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <timeZone>UTC</timeZone>
                        <fieldName>@timestamp</fieldName>
                    </timestamp>
                    <version/>
                    <logLevel>
                        <fieldName>level</fieldName>
                    </logLevel>
                    <message>
                        <fieldName>message</fieldName>
                    </message>
                    <mdc>
                        <fieldName>mdc</fieldName>
                    </mdc>
                    <arguments>
                        <fieldName>arguments</fieldName>
                    </arguments>
                    <stackTrace>
                        <fieldName>stack_trace</fieldName>
                    </stackTrace>
                    <pattern>
                        <pattern>
                            {
                                "service": "trading-service",
                                "environment": "${spring.profiles.active:-default}",
                                "version": "${spring.application.version:-unknown}",
                                "host": "${HOSTNAME:-unknown}",
                                "thread": "%thread",
                                "logger": "%logger"
                            }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
    </springProfile>
    
    <!-- File appender for all environments -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/app/logs/trading-service.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>/app/logs/trading-service.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                    <fieldName>@timestamp</fieldName>
                </timestamp>
                <version/>
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>
                <message>
                    <fieldName>message</fieldName>
                </message>
                <mdc>
                    <fieldName>mdc</fieldName>
                </mdc>
                <arguments>
                    <fieldName>arguments</fieldName>
                </arguments>
                <stackTrace>
                    <fieldName>stack_trace</fieldName>
                </stackTrace>
                <pattern>
                    <pattern>
                        {
                            "service": "trading-service",
                            "environment": "${spring.profiles.active:-default}",
                            "version": "${spring.application.version:-unknown}",
                            "host": "${HOSTNAME:-unknown}",
                            "thread": "%thread",
                            "logger": "%logger"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>
    
    <!-- Async appenders for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>2048</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <maxFlushTime>5000</maxFlushTime>
    </appender>
    
    <!-- NON-PROD PROFILE LOGGERS -->
    <springProfile name="!prod">
        <!-- Main application logger for dev/docker -->
        <logger name="com.trademaster.trading" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </logger>
        
        <!-- High-frequency trading logs (debug level in dev only) -->
        <logger name="com.trademaster.trading.execution" level="DEBUG"/>
        <logger name="com.trademaster.trading.strategy" level="DEBUG"/>
        
        <!-- Database query logs (development only) -->
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
        
        <!-- Root logger for dev -->
        <root level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- PRODUCTION PROFILE LOGGERS -->
    <springProfile name="prod">
        <!-- Main application logger for production -->
        <logger name="com.trademaster.trading" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </logger>
        
        <!-- High-frequency trading logs (info level in prod) -->
        <logger name="com.trademaster.trading.execution" level="INFO"/>
        <logger name="com.trademaster.trading.strategy" level="INFO"/>
        
        <!-- Root logger for production -->
        <root level="${LOG_LEVEL}">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- COMMON LOGGERS FOR ALL PROFILES -->
    <!-- Risk management logs -->
    <logger name="com.trademaster.trading.risk" level="INFO"/>
    
    <!-- Portfolio management logs -->
    <logger name="com.trademaster.trading.portfolio" level="INFO"/>
    
    <!-- Broker integration logs -->
    <logger name="com.trademaster.trading.broker" level="INFO"/>
    
    <!-- Spring Boot actuator logs -->
    <logger name="org.springframework.boot.actuator" level="INFO"/>
    
    <!-- Micrometer metrics logs -->
    <logger name="io.micrometer" level="INFO"/>
    
    <!-- Security related logs -->
    <logger name="org.springframework.security" level="INFO"/>
    
    <!-- Shutdown hook to ensure all logs are flushed -->
    <shutdownHook class="ch.qos.logback.core.hook.DefaultShutdownHook"/>
</configuration>